# Чистый репозиторий: только ноутбуки, отчёты и окружение.
# Игнорируем всё, затем точечно разрешаем нужное.
*

# разрешаем заходить в подпапки (иначе вложенные файлы не видны)
!*/

# окружение
!pyproject.toml
!uv.lock

# навигация
!README.md
!.gitignore

# ноутбуки и отчёты
!*.ipynb
!*_Здор.docx
